# ─── Docker Compose Environment ──────────────────────────────────
# Copy this to .env and fill in the values before running:
#   docker compose up -d

# ── Database ─────────────────────────────────────────────────────
POSTGRES_DB=soltip
POSTGRES_USER=soltip
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_PORT=5432
DB_MAX_CONNECTIONS=10

# ── Solana / Program ────────────────────────────────────────────
SOLANA_RPC_URL=https://api.devnet.solana.com
PROGRAM_ID=BhynwWdN5g5S5FfCEgDovajaYQDq925S2Xs8vXas58uo
PLATFORM_AUTHORITY=

# ── Backend ──────────────────────────────────────────────────────
BACKEND_PORT=8080
JWT_SECRET=change-me-in-production
CORS_ORIGINS=http://localhost,http://localhost:3000
AUTH_TOKEN_MAX_AGE_SECS=86400
WEBHOOK_TIMEOUT_SECS=10
WEBHOOK_MAX_RETRIES=3
COINGECKO_API_KEY=
PRICE_CACHE_TTL_SECS=300
RUST_LOG=info

# ── Frontend (build-time) ───────────────────────────────────────
FRONTEND_PORT=80
VITE_SOLANA_NETWORK=devnet
VITE_SOLANA_RPC_URL=https://api.devnet.solana.com
VITE_PROGRAM_ID=BhynwWdN5g5S5FfCEgDovajaYQDq925S2Xs8vXas58uo
VITE_API_URL=http://localhost:8080
VITE_FEATURE_POLLS=true
VITE_FEATURE_CONTENT_GATES=true
VITE_FEATURE_REFERRALS=true
VITE_FEATURE_ANALYTICS=true
VITE_FEATURE_WIDGET=true
